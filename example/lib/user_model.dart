class UserModel {
  final String? id;
  final String? name;
  final String? email;
  final int? age;
  final DateTime? createdAt;

  const UserModel({
     this.id,
     this.name,
     this.email,
     this.age,
     this.createdAt,
  });
}